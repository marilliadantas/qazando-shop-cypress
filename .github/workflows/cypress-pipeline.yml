# Nome da pipeline
name: Run basic testes

on:
    pull_request:
        branches: [main]
    
jobs:
    cypress-run:
        runs-on: ubunto-latest

        steps:
            - name: Checkout/Clone do projeto
              uses: actions/checkout@v3

            - name: Instalar dependências no terminal
              run: echo Eduardo Finnoti

            - name: Run testes
              uses: cypress-io/github-actions@v5